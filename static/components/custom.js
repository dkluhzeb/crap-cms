/**
 * User seam for bespoke admin components — empty by default.
 *
 * `index.js` auto-imports this file. The default ships empty so
 * fresh installs (no config-dir overlay) don't 404 on every admin
 * page render. Override at `<config_dir>/static/components/custom.js`
 * to register your own atoms:
 *
 *     import './my-weather-card.js';
 *     import './my-status-pill.js';
 *
 * Or add capture-phase event listeners to extend built-in components
 * without replacing them — see the admin-ui docs scenario "Replace a
 * component" for the recommended pattern.
 *
 * @module custom
 * @stability stable
 */

{{!-- partials/status-badge
     Status pill — class modifier follows the `status` value
     (e.g. `published`, `draft`, `archived`).

     Params:
       status — string used as both `badge--{status}` modifier and label --}}
<span class="badge badge--{{status}}">{{status}}</span>

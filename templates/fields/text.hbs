<label for="field-{{name}}">
  {{label}}
  {{#if required}}<span class="required">*</span>{{/if}}
  {{#if locale_locked}}<span class="form__locale-badge">{{t "shared_field"}}</span>{{/if}}
</label>
{{#if has_many}}
<crap-tags data-field-type="{{field_type}}"{{#if min_length}} data-min-length="{{min_length}}"{{/if}}{{#if max_length}} data-max-length="{{max_length}}"{{/if}}>
  <input type="hidden" name="{{name}}" value="{{value}}" />
  <div class="form__tags{{#if error}} form__tags--error{{/if}}" id="field-{{name}}">
    {{#each tags}}
    <span class="form__tags-chip" data-value="{{this}}">{{this}}<button type="button" class="form__tags-chip-remove" aria-label="Remove">&times;</button></span>
    {{/each}}
    <input
      type="text"
      class="form__tags-input"
      {{#if placeholder}}placeholder="{{placeholder}}"{{else}}placeholder="Type and press Enter"{{/if}}
      {{#if readonly}}readonly{{/if}}
      {{#if max_length}}maxlength="{{max_length}}"{{/if}}
    />
  </div>
</crap-tags>
{{else}}
<input
  type="text"
  id="field-{{name}}"
  name="{{name}}"
  value="{{value}}"
  {{#if placeholder}}placeholder="{{placeholder}}"{{/if}}
  {{#if required}}required{{/if}}
  {{#if readonly}}readonly{{/if}}
  {{#if min_length}}minlength="{{min_length}}"{{/if}}
  {{#if max_length}}maxlength="{{max_length}}"{{/if}}
  {{#if error}}class="form__input--error"{{/if}}
/>
{{/if}}
{{#if error}}<p class="form__error">{{error}}</p>{{/if}}
{{#if description}}<p class="form__help">{{description}}</p>{{/if}}

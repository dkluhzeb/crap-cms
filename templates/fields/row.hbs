<div class="form__row">
  {{#each sub_fields}}
    {{> partials/field-wrapper extra_class="form__field--row-item"}}
  {{/each}}
  {{#if error}}
    <p class="form__error">{{error}}</p>
  {{/if}}
  {{#if description}}
    <p class="form__help">{{t description}}</p>
  {{/if}}
</div>

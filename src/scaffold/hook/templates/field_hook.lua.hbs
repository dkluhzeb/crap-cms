--- {{position}} field hook for {{collection}}.{{field}}.
---@param value any
---@param context {{context_type}}
---@return any

return function(value, context)
    -- Example: return string.lower(value)

    return value
end

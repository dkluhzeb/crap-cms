--- {{position}} hook for {{collection}}.
---@param context {{context_type}}
---@return {{context_type}}

return function(context)
    -- Example: context.data.title = string.upper(context.data.title)

    return context
end

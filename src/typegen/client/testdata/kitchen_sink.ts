// Auto-generated by crap-cms. Do not edit.

/** Input data for creating/updating a N2fa */
export interface N2faData {
  type: string;
  "2fa"?: string;
}

/** N2fa document returned from the API */
export interface N2faDocument extends N2faData {
  id: string;
  created_at?: string;
  updated_at?: string;
}

/** Input data for creating/updating a Media */
export interface MediaData {
  filename: string;
}

/** Media document returned from the API */
export interface MediaDocument extends MediaData {
  id: string;
  created_at?: string;
  updated_at?: string;
}

export interface PostsSeo {
  meta_title: string;
  meta_desc?: string;
}

export interface PostsItems {
  label: string;
  meta?: PostsItemsMeta;
}

export interface PostsItemsMeta {
  key: string;
}

/** Input data for creating/updating a Posts */
export interface PostsData {
  title: string;
  status: "draft" | "published";
  author?: string | UsersDocument;
  tags?: (string | TagsDocument)[];
  cover?: string | MediaDocument;
  /** Polymorphic relationship — targets: users, tags */
  related?: (string | UsersDocument | TagsDocument)[];
  seo?: PostsSeo;
  items?: PostsItems[];
  content?: Record<string, unknown>[];
  scores?: number[];
  active?: boolean;
  data?: unknown;
}

/** Posts document returned from the API */
export interface PostsDocument extends PostsData {
  id: string;
  created_at?: string;
  updated_at?: string;
}

/** Input data for creating/updating a Tags */
export interface TagsData {
  name: string;
}

/** Tags document returned from the API */
export interface TagsDocument extends TagsData {
  id: string;
  created_at?: string;
  updated_at?: string;
}

/** Input data for creating/updating a Users */
export interface UsersData {
  name: string;
  email?: string;
}

/** Users document returned from the API */
export interface UsersDocument extends UsersData {
  id: string;
  created_at?: string;
  updated_at?: string;
}

export interface SettingsNav {
  label: string;
  url: string;
}

/** Input data for Settings global */
export interface SettingsData {
  site_name: string;
  nav?: SettingsNav[];
}

/** Settings global document */
export interface SettingsDocument extends SettingsData {
  id: string;
  created_at?: string;
  updated_at?: string;
}

/** All collection slugs */
export type CollectionSlug = "2fa" | "media" | "posts" | "tags" | "users";

// Auto-generated by crap-cms. Do not edit.
//
// Proto-to-struct conversion over the typed FieldValue/DataMap messages.
// Import the generated structs from generated.rs alongside this file.

use crate::proto::field_value::Kind;
use crate::proto::{DataMap, Document, FieldValue};

use super::generated::*;

/// Trait for types that can be constructed from a proto Document.
pub trait FromDocument {
    fn from_document(doc: &Document) -> Self;
}

// ── Extraction helpers ─────────────────────────────────────

fn get_str(doc: &Document, name: &str) -> String {
    doc.fields.as_ref()
        .and_then(|f| f.fields.get(name))
        .and_then(|v| match &v.kind {
            Some(Kind::StringValue(s)) => Some(s.clone()),
            _ => None,
        })
        .unwrap_or_default()
}

fn get_str_opt(doc: &Document, name: &str) -> Option<String> {
    let s = get_str(doc, name);
    if s.is_empty() { None } else { Some(s) }
}

fn get_num_opt(doc: &Document, name: &str) -> Option<f64> {
    doc.fields.as_ref()
        .and_then(|f| f.fields.get(name))
        .and_then(|v| match &v.kind {
            Some(Kind::IntValue(n)) => Some(*n as f64),
            Some(Kind::DoubleValue(n)) => Some(*n),
            _ => None,
        })
}

fn get_int_opt(doc: &Document, name: &str) -> Option<i64> {
    doc.fields.as_ref()
        .and_then(|f| f.fields.get(name))
        .and_then(|v| match &v.kind {
            Some(Kind::IntValue(n)) => Some(*n),
            _ => None,
        })
}

fn get_bool_opt(doc: &Document, name: &str) -> Option<bool> {
    doc.fields.as_ref()
        .and_then(|f| f.fields.get(name))
        .and_then(|v| match &v.kind {
            Some(Kind::BoolValue(b)) => Some(*b),
            _ => None,
        })
}

fn get_str_list(doc: &Document, name: &str) -> Vec<String> {
    doc.fields.as_ref()
        .and_then(|f| f.fields.get(name))
        .and_then(|v| match &v.kind {
            Some(Kind::ListValue(list)) => Some(
                list.values.iter()
                    .filter_map(|v| match &v.kind {
                        Some(Kind::StringValue(s)) => Some(s.clone()),
                        _ => None,
                    })
                    .collect()
            ),
            _ => None,
        })
        .unwrap_or_default()
}

fn get_num_list(doc: &Document, name: &str) -> Vec<f64> {
    doc.fields.as_ref()
        .and_then(|f| f.fields.get(name))
        .and_then(|v| match &v.kind {
            Some(Kind::ListValue(list)) => Some(
                list.values.iter()
                    .filter_map(|v| match &v.kind {
                        Some(Kind::IntValue(n)) => Some(*n as f64),
                        Some(Kind::DoubleValue(n)) => Some(*n),
                        _ => None,
                    })
                    .collect()
            ),
            _ => None,
        })
        .unwrap_or_default()
}


// ── JSON helpers ──────────────────────────────────────────

fn field_value_to_json(v: &FieldValue) -> serde_json::Value {
    match &v.kind {
        Some(Kind::BoolValue(b)) => serde_json::Value::Bool(*b),
        Some(Kind::IntValue(n)) => serde_json::Value::from(*n),
        Some(Kind::DoubleValue(n)) => serde_json::Number::from_f64(*n)
            .map_or(serde_json::Value::Null, serde_json::Value::Number),
        Some(Kind::StringValue(s)) => serde_json::Value::String(s.clone()),
        Some(Kind::ListValue(list)) => serde_json::Value::Array(
            list.values.iter().map(field_value_to_json).collect()
        ),
        Some(Kind::StructValue(s)) => serde_json::Value::Object(
            s.fields.iter()
                .map(|(k, v)| (k.clone(), field_value_to_json(v)))
                .collect()
        ),
        Some(Kind::NullValue(_)) | None => serde_json::Value::Null,
    }
}

fn get_json(doc: &Document, name: &str) -> Option<serde_json::Value> {
    doc.fields.as_ref()
        .and_then(|f| f.fields.get(name))
        .map(field_value_to_json)
}

fn get_json_list(doc: &Document, name: &str) -> Option<Vec<serde_json::Value>> {
    doc.fields.as_ref()
        .and_then(|f| f.fields.get(name))
        .and_then(|v| match &v.kind {
            Some(Kind::ListValue(list)) => Some(
                list.values.iter().map(field_value_to_json).collect()
            ),
            _ => None,
        })
}


// ── Relationship helpers ──────────────────────────────────

fn struct_to_document(s: &DataMap) -> Document {
    let id = s.fields.get("id")
        .and_then(|v| match &v.kind {
            Some(Kind::StringValue(s)) => Some(s.clone()),
            _ => None,
        })
        .unwrap_or_default();

    Document {
        id,
        collection: String::new(),
        fields: Some(DataMap { fields: s.fields.clone() }),
        created_at: s.fields.get("created_at").and_then(|v| match &v.kind {
            Some(Kind::StringValue(s)) => Some(s.clone()),
            _ => None,
        }),
        updated_at: s.fields.get("updated_at").and_then(|v| match &v.kind {
            Some(Kind::StringValue(s)) => Some(s.clone()),
            _ => None,
        }),
    }
}

fn get_rel<T: FromDocument>(doc: &Document, name: &str) -> Option<Rel<T>> {
    doc.fields.as_ref()
        .and_then(|f| f.fields.get(name))
        .and_then(|v| match &v.kind {
            Some(Kind::StringValue(s)) if !s.is_empty() => Some(Rel::Id(s.clone())),
            Some(Kind::StructValue(s)) => {
                let inner = struct_to_document(s);
                Some(Rel::Doc(Box::new(T::from_document(&inner))))
            }
            _ => None,
        })
}

fn get_rel_list<T: FromDocument>(doc: &Document, name: &str) -> Vec<Rel<T>> {
    doc.fields.as_ref()
        .and_then(|f| f.fields.get(name))
        .and_then(|v| match &v.kind {
            Some(Kind::ListValue(list)) => Some(
                list.values.iter()
                    .filter_map(|v| match &v.kind {
                        Some(Kind::StringValue(s)) if !s.is_empty() => Some(Rel::Id(s.clone())),
                        Some(Kind::StructValue(s)) => {
                            let inner = struct_to_document(s);
                            Some(Rel::Doc(Box::new(T::from_document(&inner))))
                        }
                        _ => None,
                    })
                    .collect()
            ),
            _ => None,
        })
        .unwrap_or_default()
}

impl N2fa {
    pub fn from_document(doc: &Document) -> Self {
        Self {
            id: doc.id.clone(),
            r#type: get_str_opt(doc, "type"),
            n2fa: get_str_opt(doc, "2fa"),
            end: get_str_opt(doc, "end"),
            private: get_str_opt(doc, "private"),
            _revision: get_int_opt(doc, "_revision"),
            created_at: doc.created_at.clone(),
            updated_at: doc.updated_at.clone(),
        }
    }
}

impl FromDocument for N2fa {
    fn from_document(doc: &Document) -> Self {
        N2fa::from_document(doc)
    }
}

impl MediaSizes {
    fn from_struct(s: &DataMap) -> Self {
        Self {
            thumbnail: s.fields.get("thumbnail").and_then(|v| match &v.kind { Some(Kind::StructValue(m)) => Some(MediaSizesThumbnail::from_struct(m)), _ => None }),
        }
    }
}

impl MediaSizesThumbnail {
    fn from_struct(s: &DataMap) -> Self {
        Self {
            url: s.fields.get("url").and_then(|v| match &v.kind { Some(Kind::StringValue(s)) => Some(s.clone()), _ => None }),
            width: s.fields.get("width").and_then(|v| match &v.kind { Some(Kind::IntValue(n)) => Some(*n as f64), Some(Kind::DoubleValue(n)) => Some(*n), _ => None }),
            height: s.fields.get("height").and_then(|v| match &v.kind { Some(Kind::IntValue(n)) => Some(*n as f64), Some(Kind::DoubleValue(n)) => Some(*n), _ => None }),
            formats: s.fields.get("formats").and_then(|v| match &v.kind { Some(Kind::StructValue(m)) => Some(MediaSizesThumbnailFormats::from_struct(m)), _ => None }),
        }
    }
}

impl MediaSizesThumbnailFormats {
    fn from_struct(s: &DataMap) -> Self {
        Self {
            webp: s.fields.get("webp").and_then(|v| match &v.kind { Some(Kind::StructValue(m)) => Some(MediaSizesThumbnailFormatsWebp::from_struct(m)), _ => None }),
        }
    }
}

impl MediaSizesThumbnailFormatsWebp {
    fn from_struct(s: &DataMap) -> Self {
        Self {
            url: s.fields.get("url").and_then(|v| match &v.kind { Some(Kind::StringValue(s)) => Some(s.clone()), _ => None }),
        }
    }
}

impl Media {
    pub fn from_document(doc: &Document) -> Self {
        Self {
            id: doc.id.clone(),
            filename: get_str_opt(doc, "filename"),
            mime_type: get_str_opt(doc, "mime_type"),
            filesize: get_num_opt(doc, "filesize"),
            width: get_num_opt(doc, "width"),
            height: get_num_opt(doc, "height"),
            url: get_str_opt(doc, "url"),
            focal_x: get_num_opt(doc, "focal_x"),
            focal_y: get_num_opt(doc, "focal_y"),
            sizes: doc.fields.as_ref().and_then(|f| f.fields.get("sizes")).and_then(|v| match &v.kind { Some(Kind::StructValue(s)) => Some(MediaSizes::from_struct(s)), _ => None, }),
            alt: get_str_opt(doc, "alt"),
            _revision: get_int_opt(doc, "_revision"),
            created_at: doc.created_at.clone(),
            updated_at: doc.updated_at.clone(),
        }
    }
}

impl FromDocument for Media {
    fn from_document(doc: &Document) -> Self {
        Media::from_document(doc)
    }
}

impl PostsSeo {
    fn from_struct(s: &DataMap) -> Self {
        Self {
            meta_title: s.fields.get("meta_title").and_then(|v| match &v.kind { Some(Kind::StringValue(s)) => Some(s.clone()), _ => None }),
            meta_desc: s.fields.get("meta_desc").and_then(|v| match &v.kind { Some(Kind::StringValue(s)) => Some(s.clone()), _ => None }),
        }
    }
}

impl PostsItems {
    fn from_struct(s: &DataMap) -> Self {
        Self {
            id: s.fields.get("id").and_then(|v| match &v.kind { Some(Kind::StringValue(s)) => Some(s.clone()), _ => None }),
            label: s.fields.get("label").and_then(|v| match &v.kind { Some(Kind::StringValue(s)) => Some(s.clone()), _ => None }),
            meta: s.fields.get("meta").and_then(|v| match &v.kind { Some(Kind::StructValue(m)) => Some(PostsItemsMeta::from_struct(m)), _ => None }),
            notes: s.fields.get("notes").and_then(|v| match &v.kind { Some(Kind::ListValue(l)) => Some(l.values.iter().filter_map(|v| match &v.kind { Some(Kind::StructValue(m)) => Some(PostsItemsNotes::from_struct(m)), _ => None }).collect()), _ => None }),
        }
    }
}

impl PostsItemsMeta {
    fn from_struct(s: &DataMap) -> Self {
        Self {
            key: s.fields.get("key").and_then(|v| match &v.kind { Some(Kind::StringValue(s)) => Some(s.clone()), _ => None }),
        }
    }
}

impl PostsItemsNotes {
    fn from_struct(s: &DataMap) -> Self {
        Self {
            body: s.fields.get("body").and_then(|v| match &v.kind { Some(Kind::StringValue(s)) => Some(s.clone()), _ => None }),
        }
    }
}

impl Posts {
    pub fn from_document(doc: &Document) -> Self {
        Self {
            id: doc.id.clone(),
            title: get_str_opt(doc, "title"),
            summary: get_str_opt(doc, "summary"),
            published_at: get_str_opt(doc, "published_at"),
            published_at_tz: get_str_opt(doc, "published_at_tz"),
            snippet: get_str_opt(doc, "snippet"),
            snippet_lang: get_str_opt(doc, "snippet_lang"),
            body: get_json(doc, "body"),
            teaser: get_str_opt(doc, "teaser"),
            scores: { let v = get_num_list(doc, "scores"); if v.is_empty() { None } else { Some(v) } },
            keywords: { let v = get_str_list(doc, "keywords"); if v.is_empty() { None } else { Some(v) } },
            active: get_bool_opt(doc, "active"),
            data: get_json(doc, "data"),
            status: get_str_opt(doc, "status").map(PostsStatus::from),
            layout: get_str_opt(doc, "layout").map(PostsLayout::from),
            categories: { let v = get_str_list(doc, "categories").into_iter().map(PostsCategories::from).collect::<Vec<_>>(); if v.is_empty() { None } else { Some(v) } },
            author: get_rel(doc, "author"),
            tags: { let v = get_rel_list(doc, "tags"); if v.is_empty() { None } else { Some(v) } },
            cover: get_rel(doc, "cover"),
            related: doc.fields.as_ref().and_then(|f| f.fields.get("related")).and_then(|v| match &v.kind { Some(Kind::ListValue(l)) => Some(l.values.iter().filter_map(|v| match &v.kind { Some(Kind::StringValue(s)) if !s.is_empty() => Some(PostsRelated::Id(s.clone())), Some(Kind::StructValue(m)) => { let __coll = m.fields.get("collection").and_then(|v| match &v.kind { Some(Kind::StringValue(s)) => Some(s.as_str()), _ => None }); let __doc = struct_to_document(m); match __coll { Some("users") => Some(PostsRelated::Doc(PostsRelatedRef::Users(Box::new(Users::from_document(&__doc))))), Some("tags") => Some(PostsRelated::Doc(PostsRelatedRef::Tags(Box::new(Tags::from_document(&__doc))))), _ => None, } }, _ => None }).collect::<Vec<_>>()), _ => None }),
            featured: doc.fields.as_ref().and_then(|f| f.fields.get("featured")).and_then(|v| match &v.kind { Some(Kind::StringValue(s)) if !s.is_empty() => Some(PostsFeatured::Id(s.clone())), Some(Kind::StructValue(m)) => { let __coll = m.fields.get("collection").and_then(|v| match &v.kind { Some(Kind::StringValue(s)) => Some(s.as_str()), _ => None }); let __doc = struct_to_document(m); match __coll { Some("users") => Some(PostsFeatured::Doc(PostsFeaturedRef::Users(Box::new(Users::from_document(&__doc))))), Some("tags") => Some(PostsFeatured::Doc(PostsFeaturedRef::Tags(Box::new(Tags::from_document(&__doc))))), _ => None, } }, _ => None }),
            seo: doc.fields.as_ref().and_then(|f| f.fields.get("seo")).and_then(|v| match &v.kind { Some(Kind::StructValue(s)) => Some(PostsSeo::from_struct(s)), _ => None, }),
            items: doc.fields.as_ref().and_then(|f| f.fields.get("items")).and_then(|v| match &v.kind { Some(Kind::ListValue(list)) => Some(list.values.iter().filter_map(|v| match &v.kind {Some(Kind::StructValue(s)) => Some(PostsItems::from_struct(s)),_ => None,}).collect::<Vec<_>>()), _ => None, }),
            content: get_json_list(doc, "content"),
            extra: get_json(doc, "extra"),
            raw_rows: get_json_list(doc, "raw_rows"),
            byline: get_str_opt(doc, "byline"),
            aside: get_str_opt(doc, "aside"),
            tab_note: get_str_opt(doc, "tab_note"),
            _revision: get_int_opt(doc, "_revision"),
            _status: get_str_opt(doc, "_status"),
            _deleted_at: get_str_opt(doc, "_deleted_at"),
            created_at: doc.created_at.clone(),
            updated_at: doc.updated_at.clone(),
        }
    }
}

impl FromDocument for Posts {
    fn from_document(doc: &Document) -> Self {
        Posts::from_document(doc)
    }
}

impl Tags {
    pub fn from_document(doc: &Document) -> Self {
        Self {
            id: doc.id.clone(),
            name: get_str_opt(doc, "name"),
            _revision: get_int_opt(doc, "_revision"),
            created_at: doc.created_at.clone(),
            updated_at: doc.updated_at.clone(),
        }
    }
}

impl FromDocument for Tags {
    fn from_document(doc: &Document) -> Self {
        Tags::from_document(doc)
    }
}

impl Users {
    pub fn from_document(doc: &Document) -> Self {
        Self {
            id: doc.id.clone(),
            email: get_str_opt(doc, "email"),
            name: get_str_opt(doc, "name"),
            authored: get_json_list(doc, "authored"),
            _revision: get_int_opt(doc, "_revision"),
            created_at: doc.created_at.clone(),
            updated_at: doc.updated_at.clone(),
        }
    }
}

impl FromDocument for Users {
    fn from_document(doc: &Document) -> Self {
        Users::from_document(doc)
    }
}

impl SettingsNav {
    fn from_struct(s: &DataMap) -> Self {
        Self {
            id: s.fields.get("id").and_then(|v| match &v.kind { Some(Kind::StringValue(s)) => Some(s.clone()), _ => None }),
            label: s.fields.get("label").and_then(|v| match &v.kind { Some(Kind::StringValue(s)) => Some(s.clone()), _ => None }),
            url: s.fields.get("url").and_then(|v| match &v.kind { Some(Kind::StringValue(s)) => Some(s.clone()), _ => None }),
        }
    }
}

impl Settings {
    pub fn from_document(doc: &Document) -> Self {
        Self {
            id: doc.id.clone(),
            site_name: get_str_opt(doc, "site_name"),
            nav: doc.fields.as_ref().and_then(|f| f.fields.get("nav")).and_then(|v| match &v.kind { Some(Kind::ListValue(list)) => Some(list.values.iter().filter_map(|v| match &v.kind {Some(Kind::StructValue(s)) => Some(SettingsNav::from_struct(s)),_ => None,}).collect::<Vec<_>>()), _ => None, }),
            _revision: get_int_opt(doc, "_revision"),
            _status: get_str_opt(doc, "_status"),
            created_at: doc.created_at.clone(),
            updated_at: doc.updated_at.clone(),
        }
    }
}

impl FromDocument for Settings {
    fn from_document(doc: &Document) -> Self {
        Settings::from_document(doc)
    }
}

